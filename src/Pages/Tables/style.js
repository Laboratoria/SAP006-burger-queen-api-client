import styled from "styled-components";

const tablesDisplay = styled.section`
    display: flex;
    justify-content: space-evenly;
`

const doubleTables = styled.div`
    display: flex;
    margin: 4vh;
`

export { tablesDisplay, doubleTables };